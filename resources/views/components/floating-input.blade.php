<div>
    <div class="form-floating mb-5">
        <input type="{{ $type }}" class="form-control" id="{{ $name }}" required
            placeholder="name@example.com" value="{{ $value }}" name="{{ $name }}">
        <label for="{{ $name }}">{{ $label }}</label>
    </div>
</div>
